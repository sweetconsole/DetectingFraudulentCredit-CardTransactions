<resources xmlns:tools="http://schemas.android.com/tools">
    <style name="Base.Theme.NFTMarketplace" parent="Theme.Material3.DayNight.NoActionBar">
        <item name="android:statusBarColor">@color/white</item>
        <item name="android:windowLightStatusBar">true</item>

        <item name="colorOnBackground">@color/white</item>              <!-- Background -->
        <item name="colorContainer">@color/faintly_grey</item>          <!-- Container -->
        <item name="colorPrimary">@color/dark_grey</item>               <!-- Title -->
        <item name="colorSecondary">@color/grey</item>                  <!-- Text -->
        <item name="colorTertiary">@color/light_grey</item>             <!-- Sub Text -->
        <item name="colorAccent">@color/violet</item>                   <!-- Accent -->
    </style>

    <style name="Theme.NFTMarketplace" parent="Base.Theme.NFTMarketplace" />
</resources>